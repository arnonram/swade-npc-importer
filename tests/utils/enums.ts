export enum Languages {
  English = 'en',
  German = 'de',
  Spanish = 'es',
  PortugueseBrazil = 'pt-BR',
}

export enum ActoryType {
  NPC = 'npc',
  Character = 'character',
}

export enum Disposition {
  Hostile = 'hostile',
  Neutral = 'neutral',
  Friendly = 'friendly',
}
